# Операционные системы
### #5 Подсистема управления памятью в операционной системе
# ПРОСТЫЕ СХЕМЫ УПРАВЛЕНИЯ ПАМЯТЬЮ
# ИЕРАРХИЯ ПАМЯТИ
Чем выше по рангу память к процессору, тем меньше время доступа, но и больше стоимость хранения.

![image](https://iili.io/fH9zZue.md.jpg)

# УПРАВЛЕНИЕ ПАМЯТЬЮ
### Принцип локальности
- Большинство реальных программ в течение некоторого отрезка времени работает с небольшим набором адресов памяти – это **принцип локальности.**
- Принцип локальности связан с особенностями человеческого мышления.
- Большинство реальных программ работает с небольшим количеством переменных.

![image](https://iili.io/fH9uEu9.jpg)

# УПРАВЛЕНИЕ ПАМЯТЬЮ
### Проблема разрешения адресов
- Человеку свойственно символическое мышление. Адреса (имена) переменных описываются идентификаторами, формируя символьное адресное пространство.
- **Проблема перехода от символьной записи к адресной.**
- Оперативная физическая память может быть представлена в виде массива ячеек с линейными адресами.
- Совокупность всех доступных физических адресов в вычислительной системе – это её физическое адресное пространство.
# Управление памятью
Рассмотрим проблему связывания адресов с символьными идентификаторами

![image](https://iili.io/fH97PWX.md.jpg)

# УПРАВЛЕНИЕ ПАМЯТЬЮ
### Проблема связывания адресов
- **Этап компиляции:** сразу после выхода из компилятора все переменные имеют окончательные адреса для выполнения в физической памяти.
- **Этап загрузки:** загрузчик может потенциально разместить переменные в произвольных местах физической памяти, но при этом двоичный образ мигрировать не может.
- **Этап выполнения:** двоичный образ может свободно перемещаться по физической памяти.

![image](https://iili.io/fH9lLj2.jpg)

# УПРАВЛЕНИЕ ПАМЯТЬЮ
### Логическое адресное пространство
- **Символьное адресное пространство** – совокупность всех допустимых идентификаторов переменных.
- **Логическое адресное пространство**– совокупность всех допустимых адресов, с которыми работает процессор.
- **Физическое адресное пространство** – совокупность всех доступных физических адресов в вычислительной системе.
# УПРАВЛЕНИЕ ПАМЯТЬЮ
### Функции ОС и hardware
- Отображение логического адресного пространства процесса на физическое адресное пространство.
- Распределение памяти между конкурирующими процессами.
- Контроль доступа к адресным пространствам процессов.
- Выгрузка процессов (целиком или частично) во внешнюю память.
- Учёт свободной и занятой памяти.
# Управление памятью
**Однопрограммная система**

В этом случае ОС мы можем разместить в:
- младших адресах RAM: программа живёт после ОС 
- старших адресах RAM: процессы размещаются в младших адресах.

![image](https://iili.io/fH9Xk12.md.jpg)

# Простые схемы Управления памятью
**Фиксированные разделы**
- Очереди для заданий формируются в зависимости от требований к размерам RAM
- Однако программы могут требовать памяти больше чем размеры, а также больше всей оперативной памяти.

![image](https://iili.io/fH9N9Ie.md.jpg)

# Простые схемы Управления памятью
### Организация больших программ
- **Оверлейная структура**
    - Программа разбивается на несколько частей. Постоянно в памяти находится только загрузчик оверлеев, небольшое количество общих данных и процедур, а части загружаются по очереди.
- **Динамическая загрузка процедур**
    - Процедуры загружаются в память только по мере необходимости, после обращения к ним.
- **Оба способа основаны на применении принципа локальности.**
# Простые схемы Управления памятью
**Динамические разделы**

- Задания из очереди поступают в память и для каждого выделяется динамический раздел.
- Используется алгоритм Round Robin с квантов времени 1 с и они выполнятся по очереди.
- После 5 с Р2 покинет систему и помещается процесс Р4.
- После нескольких циклов возникает нарезка из свободных мест.

![image](https://iili.io/fH9UZ5x.md.jpg)

# Простые схемы Управления памятью
### Динамические разделы
- **Стратегии размещения нового процесса в памяти**
    - Первый подходящий (first-fit). Процесс размещается в первое подходящее по размеру пустое место.
    - Наиболее подходящий (best-fit). Процесс размещается в наименьшее подходящее по размеру пустое место.
    - Наименее подходящий (worst-fit). Процесс размещается в наибольшее пустое место.

**Внешняя фрагментация** – невозможность использования памяти, неиспользуемой процессами из-за её раздробленности (≈ 30%).

![image](https://iili.io/fH9iWx4.md.jpg)

# Простые схемы Управления памятью
### Динамические разделы
- Для борьбы с фрагментацией памяти может помочь так называемый сборщик мусора, который может провести дефрагментацию RAM. Однако он имеет ряд недостатков:
    - Для передвижения процессов в RAM необходимо связывание адресов проводить непосредственно на этапе выполнения, когда не будет привязки к физическим адресам на программном уровне.
    - При передвижении процессов, необходима перенастройка таблиц процессов ОС, что тоже требует затрат процессорного времени.

![image](https://iili.io/fH9D8e1.jpg)

# Простые схемы Управления памятью
**Динамические разделы**
- Как организовать связывания процессов на этапе выполнения, обратимся к такому примеру.
- Из процессора выходит логический адрес, из сегментного адреса тоже выходит свой адрес и их сумма нам даст физический адрес.
- Это один из самых простых способов организации работы блока управления памятью БУП (MMU)

![image](https://iili.io/fHH9XKG.md.jpg)

# БОЛЕЕ СЛОЖНЫЕ СХЕМЫ УПРАВЛЕНИЯ ПАМЯТЬЮ
## КУСОЧНО-НЕПРЕРЫВНОЕ ОТОБРАЖЕНИЕ
### Сегментная организация памяти
- В этом случае логическое адресное пространство не является непрерывным, а разбивается на сегменты и конкретный адрес становится двумерным: т.е. мы должны определить, в каком из сегментов располагается данный адрес и на сколько от начала сегмента он смещён.
- А физическое адресное пространство так и остаётся одномерным и непрерывным. В котором мы и размещаем наши сегменты.
# КУСОЧНО-НЕПРЕРЫВНОЕ ОТОБРАЖЕНИЕ
**Сегментная организация памяти**

Из процессора выходят логические адреса. Некоторое число бит мы выделяем под номер offset с максимальным размером сегмента, а в старших битах будем располагать номер сегмента Nseg. В таблице сегментов размещается адрес начала сегмента. Чтобы получить физический адрес необходимо сложить offset и адрес сегмента из таблицы.

Однако в этом случае память тоже будет задействована не полностью. Одним из решений – размещение в таблице сегментов – их размер.

![image](https://iili.io/fHHKI5b.md.jpg)

# КУСОЧНО-НЕПРЕРЫВНОЕ ОТОБРАЖЕНИЕ
**Страничная организация памяти**

Таким образом, наше логической адресное пространство представляется в виде двух координат (номер страницы/кадра и offset). В процессе работы кадры могут быть заполнены полностью, частично или не заполнены. Свободные кадры памяти и заполняются страницами из логического адресного пространства в произвольном порядке. Для получения физического адреса необходимо задать функцию 𝑁𝑝𝑎𝑔𝑒 →𝑁𝑓𝑟𝑎𝑚𝑒. Например, в виде таблицы. 

**Свойственна и внутренняя фрагментация, но существенно ниже, чем с фиксированными разделами.**

![image](https://iili.io/fHHBhLQ.md.jpg)

# КУСОЧНО-НЕПРЕРЫВНОЕ ОТОБРАЖЕНИЕ
**Страничная организация памяти**

При данной схеме offset логического и физического адреса будет совпадать, а вот соответствие номера кадра и номера страницы мы возьмём из таблицы, в которой можно прописать и биты управления доступом.

Однако такая схема неудобна в организации работы программ программистами, поскольку возникает сложность из-за двухмерности подхода.

# КУСОЧНО-НЕПРЕРЫВНОЕ ОТОБРАЖЕНИЕ
### Сегментно-страничная организация памяти
Такой подход объединяет два предыдущих. Логическое адресное пространство разбивается на сегменты, которые разделяются на страницы, а физическое адресное пространство – на кадры. И в этом случае логический адрес будет иметь 3 координаты (Nseg, Npage, offset).

Также нам таблицу соответствия (Nseg, Np) -> Nframe

# КУСОЧНО-НЕПРЕРЫВНОЕ ОТОБРАЖЕНИЕ
**Сегментная организация памяти**

Логический адрес должен быть двумерным, старшие биты отвечают за номер сегмента. Чтобы перейти от логического к физического, вначале обращаемся к таблице сегментов и сравниваем его размер с величиной внутри soffset. Если всё нормально, то теперь берём Soffset и разбиваем его на номер страницы и Poffset. Адрес из poffset соответствует offset физического адреса. Из таблицы сегментов берём адрес, соответствующий таблице страниц, из которой берём наш номер кадра

# КУСОЧНО-НЕПРЕРЫВНОЕ ОТОБРАЖЕНИЕ
### Многоуровневая таблица страниц
- Таблица страниц процесса располагается в физическом адресном пространстве.
- При больших размерах таблицы страниц её размещение в последовательных кадрах памяти становится проблематично.
- В этом случае таблицу страниц процесса разбиваем на страницы и размещаем по кадрам в физическом адресном пространстве.
- И в этом случае мы строим таблицу страниц для таблицы страниц процесса – двухуровневая таблица страниц.
- При двухуровневой организации таблицы страниц логический адрес описывается тремя координатами.
# КУСОЧНО-НЕПРЕРЫВНОЕ ОТОБРАЖЕНИЕ
### Хешированная таблица страниц
- В этом случае берём номер страницы и подаём на ввод хеш-функции, а после в хеш-таблицу, где находятся адреса списков для кадров и страниц, удовлетворяющих одному и тому же значению списков.
- После прохождения по списку находим требуемый элемент номера страницы, соответствующий номеру кадра.
- Однако при многоуровневой организации таблицы страниц мы будем существенно терять в производительности.

![image](https://iili.io/fHHlYAv.md.jpg)

# КУСОЧНО-НЕПРЕРЫВНОЕ ОТОБРАЖЕНИЕ
**Ассоциативная память (TLB)**

Для решения проблемы с производительностью применяют решение на уровне hardware – использование ассоциативной памяти (TLB). Она очень дорогая, но крайне быстрая и в ней хранится таблица соответствия page -> frame. Если нет такого соответствия, то обратимся в таблицу страниц в оперативной памяти.

# ВИРТУАЛЬНАЯ ПАМЯТЬ

## ВИРТУАЛЬНАЯ ПАМЯТЬ
### Концепция виртуальной памяти
- Логическое адресное пространство процесса разбито на участки и линейно кусочно-непрерывно отображается на физическое адресное пространство.
- Связывание адресов происходит на этапе выполнения.
- В оперативной физической памяти одновременно размещаются не все участки логического адресного пространства, а только их часть, остальные находятся во вторичной памяти.
- При обращении к участку логического адресного пространства, находящемуся во вторичной памяти, он подкачивается в оперативную память, возможно с выталкиванием из неё некоторых неиспользующихся в данный момент участков
# ВИРТУАЛЬНАЯ ПАМЯТЬ
### Преимущества виртуальной памяти
- Процесс не ограничен объёмом физической памяти. Упрощается разработка программ.
- Повышается степень мультипрограммирования.
- Выгрузка во вторичную память части процесса происходит быстрее, чем выгрузка всего процесса. Повышается эффективность работы системы, использующей среднесрочное планирование (при использовании свопинга). 
- Однако при работе виртуальная память обращается ко вторичной, тем самым замедляя работу программ.
- Поэтому в системах реального времени, где необходимо быстрое время отклика, виртуальная память не используется.

![image](https://iili.io/fHHhMfR.jpg)

# ВИРТУАЛЬНАЯ ПАМЯТЬ
### Исключительная ситуация Page fault
- В случае, когда требуемой страницы нет в оперативной памяти, бит наличия=0, процесс завершиться не может и возникает исключение Page fault. В этом случае:
    - Выполнение команды прекращается на уровне `hardware`.
    - Сохраняется часть контекста перед выполнением команды. Управление передаётся по заранее определённому адресу – `hardware`.
    - Сохраняется оставшийся контекст – `software`.
    - Страница подкачивается в память, возможно с выталкиванием из памяти другой страницы – `software + hardware`.
    -Восстановление контекста. Повторное выполнение команды - `software + hardware`.
# ВИРТУАЛЬНАЯ ПАМЯТЬ
### Стратегии управления
- Стратегия выборки – когда подкачивать страницу?
    - по запросу
    - с упреждением.
- Стратегии размещения – куда подкачивать страницу?
    - В первый попавшийся свободный кадр.
    - В первый подходящий по размеру first-fit
    - Наиболее плотная упаковка best-fit
    - В наименее подходящий worst-fit.
- Стратегия замещения – что из RAM убрать?

![image](https://iili.io/fHHUxkv.jpg)
# ВИРТУАЛЬНАЯ ПАМЯТЬ
### Алгоритмы замещения страниц
- **Виды алгоритмов**
    - **Локальные:** процессу выделяется определённое количество кадров памяти, и только в этих кадрах он работает.
    - **Глобальные:** при работе процесса можно использовать кадры других процессов.
- Для анализа работы алгоритмов используется строка обращений процесса к памяти **(строка запросов)**
# ВИРТУАЛЬНАЯ ПАМЯТЬ
### Локальные алгоритмы замещения страниц
- FIFO (First Input – First Output).
    - *Может возникнуть ситуация «Аномалия Belady» – для некоторых строк запросов работа алгоритма при увеличении количества выделенных кадров приводит к увеличению числа страничных нарушений.*
- **Стековые алгоритмы** – алгоритмы, для которых при одной и той же строке запросов в один и тот же момент её обработки набор страниц в памяти для n кадров всегда есть подмножество набора страниц в памяти для n+1 кадра.
    - **Стековые алгоритмы не проявляют аномалии Belady.**
# ВИРТУАЛЬНАЯ ПАМЯТЬ
### Локальные алгоритмы замещения страниц
- **Стековые алгоритмы**
    - **OPT – оптимальный алгоритм:** данный алгоритм выталкивает из RAM тот процесс, к которой мы дольше всего не будем обращаться. На практике не реализуется.
    - **LRU (Least Recently Used)**: алгоритм выбрасывает страницу, которую давно не использовали.
    - **NFU (Not Frequently Used)**: выбрасывается та страница, к которой реже всего обращались.
- **«Второй шанс»:** когда страница, дольше всего пришедшая в память не выбрасывается сразу. Т.е. если мы обратились к ней ещё раз, то условие выполняется.
# ВИРТУАЛЬНАЯ ПАМЯТЬ
### Глобальные алгоритмы замещения страниц
- **Трешинг (Thrashing)**
    - В случае неправильного выделения количества кадров для процесса может возникать большое количество page fault, т.е. когда время на их обработку (подкачка/откачка в RAM) много больше, чем выполнение самих команд.
    - В случае локального замещения страниц трешинг не окажет влияние на всю ОС в целом.
    - Момент возникновения «лавинообразного» падения % использования CPU с увеличением степени мультипрограммирования принято считать **моментом возникновения трешинга**. 
    - Т.е. процесс находится в состоянии трешинга, если при его работе больше времени уходит на подкачку страниц, нежели на выполнение команд.
# ВИРТУАЛЬНАЯ ПАМЯТЬ
### Глобальные алгоритмы замещения страниц
- **Концепция рабочих множеств**
    - Основывается на принципе локальности.
    - Рабочее множество – это набор страниц, активно использующихся вместе длительное время.
    - Концепция предполагает, что во время выполнения процесс перемещается от одного рабочего множества к другому.
    - Рабочие множества определяются структурой программы и организацией данных.
    - **Модель рабочего множества** использует параметр Δ – окно рабочего множества и полную строку прошедших обращений к памяти. Количество кадров, выделяемых процессу определяется размером рабочего множества.
# ВИРТУАЛЬНАЯ ПАМЯТЬ
### Глобальные алгоритмы замещения страниц
- **Алгоритм границ**
    - В этом случае мы задаём верхнюю границу, когда частота page fault будет выше критической, выполняется команда увеличения числа кадров. 
    - При задании нижней границы, когда увеличение числа кадров не даст существенной прибавки в производительности, можем уменьшить число кадров.

# Спасибо за внимание!

![image](https://iili.io/fHJC5en.md.jpg)

    

    
    
    
    
    

    
     
    


    
    
     

    

    

    
     


    
    
     

    
    
    
    


 


 











 








    
    

  
 


 

 














 


